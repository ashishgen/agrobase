apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - port: 5173         # Port inside the cluster
      targetPort: 5173   # Port your container listens on
      nodePort: 30080    # Port exposed on all nodes
